# Local config (user copies from config.example.yaml)
config.yaml
config.local.yaml

# Personal memory file (user copies from memory.example.md)
memory.md

# Secrets / tokens
.env
.ytmd_token
*.token

# Logs
*.log

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Models / caches (Whisper model cache at repo root only — leading slash
# prevents matching the installer's C# Jarvis.Setup/Models/ folder)
.cache/
/models/

# Binaries we don't bundle
youtube-music.exe
*.exe

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# run.bat install marker
.install-stamp

# .NET build artifacts (installer/)
bin/
obj/
publish/
*.user
*.suo
.vs/

# Per-user Claude Code config (agents, hooks, local settings). Lives in
# ~/.claude/ at user scope instead; nothing project-specific to share.
.claude/
