<Window x:Class="Jarvis.Installer.NET.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Jarvis (.NET) Setup"
        Height="640" Width="840"
        MinHeight="560" MinWidth="740"
        WindowStartupLocation="CenterScreen"
        Background="#FAFAFA">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / stepper -->
        <Border Grid.Row="0" Background="#1F1F2E" Padding="20,16">
            <StackPanel>
                <TextBlock Text="Jarvis (.NET)" Foreground="White"
                           FontSize="22" FontWeight="SemiBold"/>
                <TextBlock x:Name="StepLabel" Foreground="#CCC"
                           FontSize="12" Margin="0,2,0,0"/>
            </StackPanel>
        </Border>

        <!-- Page host -->
        <Frame x:Name="PageHost" Grid.Row="1"
               NavigationUIVisibility="Hidden"
               Background="White"/>

        <!-- Nav buttons -->
        <Border Grid.Row="2" Background="#EFEFEF" Padding="20,12"
                BorderBrush="#DDDDDD" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="CancelBtn" Content="Cancel"
                        HorizontalAlignment="Left"
                        Click="CancelBtn_Click"/>
                <StackPanel Orientation="Horizontal" Grid.Column="1">
                    <Button x:Name="BackBtn" Content="Back"
                            IsEnabled="False" Click="BackBtn_Click"/>
                    <Button x:Name="NextBtn" Content="Next"
                            IsDefault="True" Click="NextBtn_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
